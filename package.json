{
  "name": "api-condominios",
  "version": "1.0.0",
  "description": "API de Condomínios",
  "scripts": {
    "build": "pip install -r requirements.txt"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0"
  }
} 