{
  "name": "api-condominios",
  "version": "1.0.0",
  "description": "API de Condomínios",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p public && pip install -r requirements.txt",
    "start": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^1.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
} 